// This file is intentionally left minimal.
// expo-router uses `expo-router/entry` as the main entry point (see package.json).
// All navigation and screen logic lives in the app/ directory.
export {};
